{
  "name": "careerpath-pro",
  "version": "1.0.0",
  "description": "CareerPath Pro - Professional Career Counseling Platform for College Students",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "build:netlify": "npm install && cd client && npm install && npm run build && cd ../server && cp package-netlify.json ../netlify/functions/package.json",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install",
    "netlify-build": "cd client && npm install && npm run build"
  },
  "keywords": ["career", "counseling", "education", "react", "nodejs", "assessment", "professional", "students", "guidance"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}