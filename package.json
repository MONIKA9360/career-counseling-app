{
  "name": "career-counseling-app",
  "version": "1.0.0",
  "description": "CareerGuide - Career Counseling Web Application for College Students",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "vercel-build": "cd client && npm install && npm run build",
    "build:netlify": "npm install && cd client && npm install && npm run build && cd ../server && cp package-netlify.json ../netlify/functions/package.json",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install",
    "netlify-build": "cd client && npm install && npm run build"
  },
  "keywords": ["career", "counseling", "education", "react", "nodejs"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.7",
    "express-validator": "^7.0.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}